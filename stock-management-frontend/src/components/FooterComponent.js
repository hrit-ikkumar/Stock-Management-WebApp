import { render } from '@testing-library/react';
import React, {Component} from 'react';

const Footer = () => {
    return(
        <div>
            <h4>Footer</h4>
        </div>
    );
}

export default Footer;